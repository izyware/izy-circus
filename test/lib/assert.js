var modtask = function() {}
modtask.status = function(serverOutcome, expectedCode) {
  var testFramework = modtask.__modtask;
  if (!expectedCode) expectedCode = 200;
  // if (serverOutcome.status === expectedCode)
}

